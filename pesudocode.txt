PROGRAM: Patient Registration Portal

START
  DISPLAY redirect page (index.html)
  AFTER 3 seconds → OPEN patient-form.html

  DISPLAY banner with company name and current date
  SHOW patient registration form with grouped sections

  WAIT for user input across all required fields:
      - Patient info (name, DOB, SSN, phone)
      - Address
      - Emergency contact
      - Medical details
      - Account credentials

  WHEN "Review" button clicked:
      GATHER all input values
      VALIDATE:
          DOB format (MM/DD/YYYY)
          DOB not in future or >120 years old
          Email contains "@" and "."
          ZIP code matches 5 or 9 digits
          Password and Confirm match
      MASK sensitive fields (SSN, Password)
      DISPLAY formatted 3-column table:
          LABEL | VALUE | STATUS (pass/error)
      INCLUDE medical history Y/N and vaccination results

  WHEN "Submit" button clicked:
      IF all required fields valid THEN
          REDIRECT to thankyou.html
      ELSE
          DISPLAY error message

  WHEN "Clear" clicked:
      RESET form and hide review area

END PROGRAM
